---
title: "2.퀵정렬"
firstDate: "2021-03-29"
lastDate: "2021-03-29"
---

## 소스코드

개념적 코드

```Python
def quickSort(array):
    if len(array) < 2: # 요소가 1개라면 정렬 불필요
        return array
    else:
        pivot = array[0] #첫요소를 pivot으로 설정
        less = [i for i in array[1:] if i <= pivot]
        greater = [i for i in array[1:] if i > pivot]
        return quickSort(less) + [pivot] + quickSort(greater)
```
